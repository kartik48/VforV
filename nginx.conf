server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Support for HEIC images
    types {
        image/heic heic;
        image/heif heif;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|HEIC|heic|mov|MOV)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Disable cache for HTML
    location ~* \.html$ {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }
}
